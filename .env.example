# Airtable — see https://airtable.com/create/tokens for a personal access token.
# The base/table IDs below are the user's real "Taipei Venue Search" > "Listings"
# IDs — safe to commit (they are not secrets on their own). Only the token is
# sensitive, so keep your real .env out of git.
AIRTABLE_TOKEN=patXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=appVHvHkL5bW7QT2o
AIRTABLE_TABLE_ID=tblKjxqWdCKZNzkDN
